<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/menu_picture"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scaleType="centerCrop"
            android:src="@drawable/placeholder"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_weight="0" />

        <TextView
            android:id="@+id/recipe_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginTop="10dp"
            android:text="@string/product_name"
            android:textSize="15sp"
            android:textStyle="italic|bold"
            app:layout_constraintEnd_toStartOf="@+id/nutriscore"
            app:layout_constraintStart_toStartOf="@+id/menu_picture"
            app:layout_constraintTop_toBottomOf="@+id/menu_picture"
            app:layout_constraintVertical_bias="0.7"
            app:layout_constraintVertical_weight="1" />

        <ImageView
            android:id="@+id/nutriscore"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:src="@drawable/nutri_score_b"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/recipe_name"
            app:layout_constraintTop_toTopOf="@+id/recipe_name"
            app:layout_constraintVertical_bias="0.3" />

    </ScrollView>


</androidx.constraintlayout.widget.ConstraintLayout>